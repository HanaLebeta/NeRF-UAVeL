name: nerf-uavel
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - numpy
  - scipy
  - matplotlib
  - tqdm
  - pyyaml
  - scikit-learn
  - pillow
  - pip
  - pip:
    - torch>=1.12.0
    - torchvision>=0.13.0
    - tensorboard>=2.9.0
    - open3d>=0.15.0
    - trimesh>=3.12.0
    - einops>=0.6.0
